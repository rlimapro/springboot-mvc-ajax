spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/promospring
    username: postgres
    password: postgres
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
  thymeleaf:
    cache: false
